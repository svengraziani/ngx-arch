import { NgxaIsAuthenticatedDirective } from './is-authenticated.directive';

describe('IsAuthenticatedDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxaIsAuthenticatedDirective();
    expect(directive).toBeTruthy();
  });
});
